[package]
name = "quorum-gate-guest"
description = "SPEL LEZ program that verifies Quorum threshold proofs on-chain"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[workspace]

[[bin]]
name = "quorum_gate"
path = "src/bin/quorum_gate.rs"

[dependencies]
quorum-gate-core = { path = "../../../crates/quorum-gate-core" }
nssa_core = { git = "https://github.com/logos-blockchain/logos-execution-zone.git", tag = "v0.2.0", package = "lee_core" }
risc0-zkvm = { version = "=3.0.5", default-features = false, features = ["std"] }
serde = { version = "1", features = ["derive"] }
spel-framework = { git = "https://github.com/logos-co/spel.git", rev = "0cb7e0980535af619482cf1c823f4d394b3ebd61" }
