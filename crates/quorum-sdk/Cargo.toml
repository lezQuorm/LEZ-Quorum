[package]
name = "quorum-sdk"
description = "Client-side Quorum toolkit: member sets, proposal lifecycle, approval proofs (LP-0002)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
quorum-core = { path = "../quorum-core" }
quorum-circuit = { path = "../quorum-circuit" }
quorum-gate-core = { path = "../quorum-gate-core" }
quorum-prover = { path = "../quorum-prover" }
quorum-threshold-methods = { path = "../../guests/quorum-threshold" }
risc0-zkvm.workspace = true
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bincode = "1.3"
rand = "0.8"
thiserror = "2"

[lints]
workspace = true
